{
  "name": "@figma-bridge/plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node esbuild.config.mjs",
    "bundle": "node esbuild.config.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@figma-bridge/shared": "workspace:*"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.0"
  }
}
